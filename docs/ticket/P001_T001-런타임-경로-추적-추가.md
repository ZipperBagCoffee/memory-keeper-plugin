# P001_T001 - SKILL.md + inject-rules.js 런타임 실행 검증 추가

## Parent
- Plan: P001 - 런타임 실행 검증을 워크플로우 검증에 추가

## Intent (의도)
P001의 전체 7단계를 한 세션에서 실행. 워크플로우의 검증 정의에 "런타임 실행 검증"을 4번째 필수 요소로 추가하여, Work Agent/Review Agent/Orchestrator 모든 레이어에서 코드가 실제로 실행되는 경로를 추적하도록 의무화한다.

## Scope (범위)
할 것:
- skills/workflow/SKILL.md Phase 8, 9, 10 수정
- skills/workflow/SKILL.md 역할 테이블, Anti-pattern 추가
- scripts/inject-rules.js RULES "Verification standard" 강화
- CLAUDE.md 자동 동기화 확인

안 할 것:
- 버전 범프 (별도 작업)
- 다른 스킬 파일 변경
- 새 파일 생성

## Acceptance Criteria (완료 조건)
- [ ] Phase 8에 Work Agent 런타임 실행 검증 의무 절차 존재
- [ ] Phase 9에 런타임 실행 검증 포맷 및 Review Agent 검증 절차 존재
- [ ] Phase 10 Self-enforcement Checklist에 런타임 실행 검증 확인 항목 존재
- [ ] "What Each Role Needs" 테이블 3개 역할 모두 런타임 추적 언급
- [ ] Anti-pattern 목록에 런타임 실행 검증 관련 항목 존재
- [ ] inject-rules.js RULES에 런타임 실행 검증 의무화 명시
- [ ] CLAUDE.md에 inject-rules.js RULES 변경 반영 (자동 동기화)

## Verification (검증)
Phase 8 절차: SKILL.md Phase 8 섹션을 읽고, "구현 후 런타임 실행 경로를 추적하라"는 의무가 Work Agent에게 부여되어 있는지 확인. 빠뜨릴 수 없는 구조인지 mental execution.
Phase 9 절차: Phase 9 섹션에 런타임 실행 검증 포맷(엔트리포인트→호출체인→최종결과)이 정의되어 있고, Review Agent가 이를 PASS/FAIL로 판정하는 절차가 있는지 확인.
Phase 10 절차: Self-enforcement Checklist에 "런타임 실행 검증 확인" 항목이 있고, Orchestrator가 이를 건너뛸 수 없는 구조인지 확인.
역할 테이블: 3개 역할 행 모두 런타임 관련 텍스트 포함 확인.
Anti-pattern: 번호가 부여된 새 항목이 존재하고, "런타임 실행 검증 없이 검증 완료"를 금지하는 내용인지 확인.
inject-rules.js: RULES 상수의 "Verification standard" 항목에 런타임 실행 검증이 명시되어 있는지 확인.
CLAUDE.md 동기화: inject-rules.js 실행 후 CLAUDE.md의 RULES 섹션이 inject-rules.js RULES와 일치하는지 확인 (node 실행으로 검증).

## Execution
- 이 티켓은 단독 워크플로우로 실행 (1 Ticket = 1 Workflow)
- 실행: `/workflow` 스킬 호출

## Log

---
### [2026-03-15 20:53] 생성
P001 전체 7단계를 1티켓으로 실행. 대상 파일: skills/workflow/SKILL.md, scripts/inject-rules.js. D001 결론 + P001 승인 기반.
